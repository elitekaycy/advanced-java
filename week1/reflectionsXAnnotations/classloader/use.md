# run in the current classloader directory (compile)

```
 javac -d out *.java
```

# run in the current class loader directoru (run)

```
java -cp out week1.reflectionsXAnnotations.classloader.CustomClassLoaderTestMain
```
